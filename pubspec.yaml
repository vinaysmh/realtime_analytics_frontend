name: realtime_analytics_frontend
description: "A Real-Time Analytics Dashboard"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  intl:
  http:
  fl_chart:
  flutter_bloc:
  web_socket_channel:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
